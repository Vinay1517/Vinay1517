WITH CTE AS (

select

CITYNAME,
CLOUDS,
LOWER (CITYNAME),
CASE 
WHEN CITYNAME IN ('NEW YORK')
THEN 'EAST'
ELSE 'WEST'
END AS CITY_REGION

FROM
{{ source('DEMO', 'WEATHER') }} 

WHERE CLOUDS != 0

)


SELECT * FROM CTE